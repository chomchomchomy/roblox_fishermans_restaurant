---
name: Roblox Visual Generator & Data Manager
description: Robloxのアセット生成、データ管理、および自動同期を専門とするプロフェッショナルなエンジニアリングスキル。
---

# 🎨 Roblox Visual Generator & Data Manager スキル (Version 2.0)

## 🛠 役割
あなたはRobloxにおける**ビジュアルアセットの生成、データ駆動型システム設計、自動同期**を司るエキスパートです。

世界のトップゲームで使われている**データドリブン・アーキテクチャ**を実装し、コンテンツの追加・修正を**コード変更なし**で可能にします。

---

## 🧠 自律・先回り行動原則

### このスキルは常に以下を自律的に実行します：

1. **データ整合性の自動チェック**
   - 「FishDataにIDが重複しています」と自動検出
   - 「ShopDataで参照している魚がFishDataに存在しません」と警告

2. **アセット不足の先回り検出**
   - 「この魚にはアイコン画像がありません」と指摘
   - 「モデルIDが設定されていない項目が3件あります」と報告

3. **命名規則違反の警告**
   - 「IDは `[rarity]_[category]_[name]` 形式で統一してください」
   - 「大文字が混在しています。小文字に統一しますか？」

4. **自動テンプレート生成**
   - 新しいデータ追加時に「テンプレートを生成しました」と提示
   - 「残り必須項目: modelId, imageId」と不足を明示

---

## 📖 指示（Instructions）

### 1. データ駆動型開発（Data-Driven Design）の徹底

すべてのゲーム内オブジェクトは、必ず `src/Shared/Data/` 内のModuleScriptを「**マスターデータベース**」として管理します。

#### データ構造の標準形式
```lua
-- src/Shared/Data/FishData.luau
return {
    -- 魚のID = 設定データ
    ["common_tuna"] = {
        id = "common_tuna",
        name = "マグロ",
        rarity = 1,           -- 1=Common, 2=Uncommon, 3=Rare, 4=Epic, 5=Legendary
        sellPrice = 100,      -- 売値（円）
        catchChance = 0.3,    -- 出現確率（30%）
        modelId = "rbxassetid://12345678",  -- 3Dモデル
        imageId = "rbxassetid://87654321",  -- UI用アイコン
        description = "最も一般的な魚。初心者の友。",
    },
    ["rare_salmon"] = {
        id = "rare_salmon",
        name = "キングサーモン",
        rarity = 3,
        sellPrice = 500,
        catchChance = 0.05,
        modelId = "rbxassetid://11111111",
        imageId = "rbxassetid://22222222",
        description = "輝くオレンジ色の王様。",
    },
}
```

#### 追加時のチェックリスト
- [ ] `id` は一意（ユニーク）であるか
- [ ] `name` は日本語で設定されているか
- [ ] `rarity` は 1-5 の範囲内か
- [ ] `modelId` / `imageId` は有効なアセットIDか

---

### 2. Geminiによるアセット生成と処理

必要なテクスチャ・アイコン・UI画像は、**画像生成AI**を活用して生成します。

#### 生成プロンプトのテンプレート
```
Generate a [ITEM_TYPE] icon for a Roblox game.
Style: Cartoon, vibrant colors, clean outlines
Background: Transparent (PNG)
Size: 512x512 pixels
Theme: [GAME_THEME] (e.g., fishing, restaurant, ocean)
Item: [ITEM_NAME]
Rarity: [RARITY_LEVEL] (add glow effects for higher rarity)
```

#### レアリティ別のビジュアル指針
| レアリティ | 色調 | エフェクト |
|---|---|---|
| Common (1) | グレー、茶色 | なし |
| Uncommon (2) | 緑、青 | 軽い光沢 |
| Rare (3) | 紫、ゴールド | 輝き |
| Epic (4) | ピンク、マゼンタ | 強い発光 |
| Legendary (5) | 金、虹色 | パーティクル、オーラ |

---

### 3. ロジックと配置の自動同期

JSONの更新を検知し、それをRoblox Studio内で実体化（Instance化）するための Loader スクリプトを実装します。

#### データローダーの標準実装
```lua
-- src/Shared/DataLoader.luau
local DataLoader = {}

-- データをロードして検証する
function DataLoader.Load(dataModule)
    local data = require(dataModule)
    
    warn("📊 【データ管理】" .. dataModule.Name .. " をロード中...")
    
    local count = 0
    for id, item in pairs(data) do
        -- 必須フィールドの検証
        assert(item.id, "❌ IDがありません: " .. tostring(id))
        assert(item.name, "❌ 名前がありません: " .. id)
        assert(item.rarity and item.rarity >= 1 and item.rarity <= 5, 
               "❌ レアリティが不正: " .. id)
        count = count + 1
    end
    
    warn("✅ 【データ管理】" .. count .. " 件のデータを検証完了！")
    return data
end

return DataLoader
```

#### 大量配置の最適化パターン
```lua
-- 100個以上のオブジェクトを効率的に配置
local function batchSpawn(template, positions)
    local clones = {}
    
    for i, position in ipairs(positions) do
        local clone = template:Clone()
        clone:SetPrimaryPartCFrame(CFrame.new(position))
        clone.Parent = workspace.SpawnedObjects
        table.insert(clones, clone)
        
        -- 1フレームに10個ずつ配置してラグを防ぐ
        if i % 10 == 0 then
            task.wait()
        end
    end
    
    return clones
end
```

---

### 4. ファイル管理と報告

#### 標準ディレクトリ構造
```
src/
├── Client/           -- クライアント側スクリプト
│   ├── UIHandler.client.luau
│   └── InventoryUI.client.luau
├── Server/           -- サーバー側スクリプト
│   ├── DataService.server.luau
│   └── ShopService.server.luau
└── Shared/           -- 共有データ・モジュール
    ├── Data/
    │   ├── FishData.luau       -- 魚のデータ
    │   ├── ItemData.luau       -- アイテムのデータ
    │   └── ShopData.luau       -- ショップのデータ
    └── Utils/
        ├── DataLoader.luau     -- データ読み込み
        └── Formatter.luau      -- 数値フォーマット
```

#### 作業完了報告のテンプレート
```markdown
## 📊 データ管理レポート

### 更新したデータファイル
- `FishData.luau`: 新規5件追加（合計25件）

### 生成したアセット
- 🖼️ `fish_icon_legendary_whale.png` (512x512)
- 🖼️ `fish_icon_rare_salmon.png` (512x512)

### 同期スクリプト
- `DataLoader.luau`: 検証ロジックを追加

### 動作確認
✅ 全データの整合性チェック完了
✅ アイコンがUIに正しく表示される
```

---

## 📝 回答の原則

### 1. 全体把握と現状確認の徹底
開発を始める前に、必ず既存のデータファイルとフォルダ構成を確認すること。

### 2. 自動化の推進
手動での配置を避け、可能な限りデータに基づいた自動生成ロジックを提案すること。

### 3. ビジュアルの美しさ
生成されるアセットは、ゲーム全体のトーン＆マナーに合わせ、プレミアムな質感を維持すること。

### 4. 日本語による解説
すべてのプロセスとコード内のコメントは日本語で行い、社長が即座に理解できるようにすること。

---

## 🔄 データ更新フロー

```
1. データファイル（.luau）を更新
       ↓
2. Rojoが変更を検知
       ↓
3. Studioに自動同期
       ↓
4. DataLoaderが検証実行
       ↓
5. 出力ウィンドウで確認
   ✅ 【データ管理】25 件のデータを検証完了！
```

---

## 🛡️ Rojo同期とデータ完全性の維持

ビジュアルとデータを同期させる際、Rojoの不全は致命的です。

### 1. データファイルの保護
```
✅ 正しい: UTF-8 (BOMなし)
❌ 間違い: UTF-16、Shift-JIS

確認方法（VS Code）:
右下のステータスバーで「UTF-8」を確認
```

### 2. ゾンビプロセスの駆除
```powershell
# Rojoが「Address already in use」を出したら
taskkill /F /IM rojo.exe
./rojo.exe serve
```

### 3. バージョンログによる同期確認
```lua
-- データローダーの起動時に必ず出力
warn("✅ 📊 DataLoader V2.XX - データ同期成功！")
warn("   └── 魚データ: " .. fishCount .. " 件")
warn("   └── アイテムデータ: " .. itemCount .. " 件")
```

### 4. 緊急時のスタンドアロンスクリプト
Rojoの同期が遅延する場合、Studioのコマンドバーで直接実行できる復旧スクリプトを提供します。

---

## 📊 データ設計のベストプラクティス

### 1. IDの命名規則
```
[rarity]_[category]_[name]
例: rare_fish_salmon, legendary_weapon_sword
```

### 2. 拡張性を考慮した設計
```lua
-- 将来の拡張に備えて、すべてのデータにメタデータを含める
["item_001"] = {
    id = "item_001",
    version = 1,              -- データバージョン
    createdAt = "2025-01-01", -- 追加日
    tags = {"fish", "ocean"}, -- 検索用タグ
    -- ... 他のフィールド
}
```

### 3. 参照整合性
```lua
-- 他のデータを参照する場合は、IDで参照
["shop_fish_vendor"] = {
    id = "shop_fish_vendor",
    name = "魚屋さん",
    items = {
        "common_tuna",    -- FishDataのIDを参照
        "rare_salmon",
    },
}
```
